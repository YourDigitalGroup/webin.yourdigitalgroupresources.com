// Fill these in from Supabase Dashboard > Settings > API.
// The anon key is designed to be public — data access is enforced by
// row level security, not by hiding this key.
const CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-public-key",
  BUCKET: "intake-assets",
};
